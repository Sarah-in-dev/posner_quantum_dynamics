# PRE-REGISTRATION — PO-5 UNIT 11 · the three repair arms

**Registered 2026-07-19 BEFORE the run. Sarah's directive.** All arms opt-in; all-off verified
**bit-identical** (`1034 / 369740 / 0.991922159684`).

## The arms

| arm | change | rationale |
|---|---|---|
| **A — coupling length** | sweep `coupling_length` (diagnostic override, Tier 2, no constant edited) | It was *"declared, never read"* on 2026-05-29 (`model6-research-findings-may29:129`), was wired into the live rate later, and has **no citation anywhere**. A dormant ungrounded constant became load-bearing. Sweeping asks whether it is load-bearing; it does **not** assert a correct value. |
| **B — degree cap** | cap birth-partners at k (nearest in birth time) | Fisher is **pairwise**: one pyrophosphate → two phosphates → one singlet. Coincidence is not an entangling operation and entanglement is not transitive, so all-to-all bonding manufactures edges with no physical origin. **True per-event matching is impossible here** — dimers are born from a concentration field (`:210-213`), with no molecular provenance. k=4 is the Fisher-consistent degree for Ca₆(PO₄)₄. |
| **C — J-compatibility on formation** | bond only if min\|ΔJ\| ≤ tol | Unit 6: dissolution is inert, so gating must act on **formation**. Routes the ℝ⁶ `j_couplings_intra` (collapsed to std/mean at `:310-311`, never seen by the entanglement layer) into the bond decision. **UNVERIFIED PHYSICS** — that intra-dimer J gates inter-dimer entanglement is an assumption. |

## Predictions

- **P1.** Baseline (all off) reproduces `largest_frac ≈ 1.0` at native field. Sanity anchor.
- **P2.** Arm B fragments the graph: `largest_frac < 0.9` at k=4. **If it does not, sparsifying the
  clique is not sufficient and the percolation is carried by P2 alone** (consistent with Unit 7).
- **P3.** Arm A: if `largest_frac` is insensitive across the swept range, the constant is **not**
  load-bearing and the provenance gap is harmless; if sensitive, it is load-bearing and ungrounded.
- **P4.** Arm C changes the bond set at all (a non-null delta vs baseline). Unit 6's mechanism
  failed by acting on an inert term; this checks the formation term is not also inert.

**A fragmenting arm is NOT thereby endorsed.** These are experiments. No arm's value is nominated
as correct, and B and C encode physics claims that need grounding independent of any result here.

## Limits
Single synapse, 1 s, 2 seeds/cell, native field unless swept. Arm B's degree cap is a **proxy** for
per-event matching, not the mechanism itself.
