# Lead: po3-einvasion (PO-3 · E_invasion provenance + the ratchet) — OWNED BY THIS PO

**Objective (the done-bar, a MEASUREMENT):** `r` measured across N traversals showing ratchet
or no-ratchet — pre-registered, with a null arm and a verdict function that can return
FALSIFIED and INCONCLUSIVE, with its positive control demonstrated to fire — plus a
provenance verdict on `k_polymerization_max` (and `E_ref`, which sits in the load-bearing
denominator at `spine_plasticity_module.py:411-412`).

**Status:** DISPATCHED by the MO 2026-07-18 17:35Z — chip `task_ead7d588` pending.
**Current unit:** — (first act is the `### GROUNDING BRIEF`)
**Last heartbeat:** —
**Blocked on:** —

**HARD STOP — the negative branch is Sarah's call.** This PO MEASURES and STOPS. No remedy,
no constant adjusted, no protocol extended to rescue it. Board-level decision.

**Compute cap:** ONE backgrounded run, per-traversal progress output, `r`-vs-traversal
persisted incrementally so a kill costs nothing. Never piped through `tail`. Raise once with a
stated reason in the queue file; beyond that, escalate.

**Owns:** the actin / `E_invasion` block in `spine_plasticity_module.py`, its `sweep/` probe.
**Must not touch:** PO-1's `vibrational_cascade_module.py` + backbone params (**PO-1 is editing
them concurrently**) · PO-2's `atp_system.py` / phosphate path · PO-4's `analytical_gap`,
`run_theta_burst_45s.py` · PO-5's `multi_synapse_network.py`, T1′ family · PO-6's surfaces.

**Unblocks:** PO-5 (HARD). At η = 0 there is no partition to be selective — L·ETA-3 measured
zero cross-synapse edges for the whole trial.

**Notes:** the ratchet prediction (~89% retention across a 20 s gap) comes from the GROUNDED
constant `tau_extrude = 180 s` (Honkura 2008), not the inherited `k_polymerization_max = 0.1`.
Read `model6-input-engine` in full first — the ERR-2 scar is that a measurement of this exact
class was dispatched without it and had to be retracted.
