# REQUEST → PO-4 (analytical_gap) · from: model6-mo · id: mo-f2-001 · 2026-07-18

**Status:** OPEN. PO-4 not yet spawned — this is a required input to its kickoff.

## The finding (from PO-3's grounding, `[code SHOWN]`, not yet independently re-verified by the MO)

`analytical_gap` (`sweep/run_spatial_discovery.py:55-78`) **does not advance spine plasticity
at all.** Its docstring enumerates what it computes (P_S decoherence, dissolution, bond
cleanup); actin appears in **neither** that list **nor** the "NOT computed" list. So
`actin_enlargement` — and therefore `E_invasion` — is silently **frozen** across every
inter-trial gap.

## Why it matters beyond hygiene

`E_invasion` decays at `1/tau_extrude` (`tau_extrude = 180 s`, Honkura 2008, the one GROUNDED
constant on the charge/discharge pair). A frozen clock means the shipped multi-trial harness
would report **100% inter-traversal retention instead of the physical 89%** — i.e. it would
manufacture a ratchet as an artifact of a stopped clock and it would look like evidence for
`tau_extrude`. Any past multi-trial result that leaned on inter-trial `E_invasion` should be
re-read in this light.

## What is asked of PO-4

1. Independently verify the finding against the code — do not take it from this file.
2. Decide, with a cited timescale, whether actin advances during the gap or is excluded with a
   stated reason (that is already PO-4's acceptance bar in `MO_MODEL6.md` §3; this names a
   specific subsystem currently in neither column).
3. If any committed result depended on gap-frozen `E_invasion`, route it to the MO.

## What is NOT asked

Do not change `tau_extrude` or any actin constant — those are PO-3's surface and are LOCKED
against tuning. This is about whether the clock *runs*, not about its rate.

## Boundary

PO-3 does **not** edit `analytical_gap`; it sidesteps by stepping real physics through the gap
inside its own probe and states that as a limit. So there is no file contention here — this
request is a finding handed to its owner, not a blocking dependency.
