# REQUEST → PO-4 (analytical_gap) · from: model6-mo · id: mo-f2-001 · 2026-07-18

**Status:** OPEN. PO-4 not yet spawned — this is a required input to its kickoff.

## The finding (from PO-3's grounding, `[code SHOWN]`, not yet independently re-verified by the MO)

`analytical_gap` (`sweep/run_spatial_discovery.py:55-78`) **does not advance spine plasticity
at all.** Its docstring enumerates what it computes (P_S decoherence, dissolution, bond
cleanup); actin appears in **neither** that list **nor** the "NOT computed" list. So
`actin_enlargement` — and therefore `E_invasion` — is silently **frozen** across every
inter-trial gap.

## Why it matters beyond hygiene

`E_invasion` decays at `1/tau_extrude` (`tau_extrude = 180 s`, Honkura 2008, the one GROUNDED
constant on the charge/discharge pair). A frozen clock means the shipped multi-trial harness
would report **100% inter-traversal retention instead of the physical 89%** — i.e. it would
manufacture a ratchet as an artifact of a stopped clock and it would look like evidence for
`tau_extrude`. Any past multi-trial result that leaned on inter-trial `E_invasion` should be
re-read in this light.

## What is asked of PO-4

1. Independently verify the finding against the code — do not take it from this file.
2. Decide, with a cited timescale, whether actin advances during the gap or is excluded with a
   stated reason (that is already PO-4's acceptance bar in `MO_MODEL6.md` §3; this names a
   specific subsystem currently in neither column).
3. If any committed result depended on gap-frozen `E_invasion`, route it to the MO.

## What is NOT asked

Do not change `tau_extrude` or any actin constant — those are PO-3's surface and are LOCKED
against tuning. This is about whether the clock *runs*, not about its rate.

## Boundary

PO-3 does **not** edit `analytical_gap`; it sidesteps by stepping real physics through the gap
inside its own probe and states that as a limit. So there is no file contention here — this
request is a finding handed to its owner, not a blocking dependency.

---

## MO ADDENDUM — 2026-07-18 17:50Z (verified independently; PO-3's finding CONFIRMED and WIDENED)

The MO re-verified F-2 against the code rather than accepting PO-3's report. **Confirmed
exactly:** `sweep/run_spatial_discovery.py:58-78` lists five computed items (P_S decoherence,
dissolution, particle removal, bond cleanup, stochastic disentanglement) and six NOT-computed
items (calcium, ATP/phosphate, EM field, new dimer formation, new bond formation, the O(n²)
tracker recalc). **Actin / spine plasticity / `E_invasion` appears in NEITHER list.** Silently
frozen, as PO-3 said.

**But `analytical_gap` is DUPLICATED, and PO-3's finding names only one copy:**

| copy | path |
|---|---|
| 1 | `sweep/run_spatial_discovery.py:55` |
| 2 | `src/models/Model_6/sweep/run_theta_burst_45s.py:44` |

Both are PO-4's surface. **The frozen-actin defect is in both.** A fix applied to one leaves the
other live — which is *precisely* the failure the audit already recorded as item 16: *"the
2026-07-18 fix covered `sweep/run_spatial_discovery.py` only — **a gap in that fix**."* The same
partial-fix shape was about to recur on the same pair of files. **PO-4 must diff the two copies
first and fix both, or consolidate them.**

**Navigation hazard PO-4 needs before it starts.** There are TWO `sweep/` trees and they are not
copies: `./sweep/` (19 entries, repo root) and `./src/models/Model_6/sweep/` (26 entries).
Eighteen files exist ONLY in the repo-root tree, including `run_spatial_discovery.py`,
`test_learning_pathway.py`, `verify_metabolic_wiring.py`, `presynaptic_release.py` and
`phosphate_conservation_probe.py`. **A bare `sweep/...` file:line reference is ambiguous across
this repo** — the MO hit this resolving PO-3's citation. Every path in a PO return must be
repo-root-relative. This also means audit item 16's two paths are two different trees, not a
typo.

**K_CLASSICAL, for the record (MO-owned, still parked, NOT PO-4's to decide):** the spread is
confirmed live — `0.05` at `sweep/run_spatial_discovery.py:80` (the gap), `0.005` at
`ca_triphosphate_complex.py:160` and `sweep/phosphate_conservation_probe.py:69`, `0.001` at
`dimer_particles.py:127`. PO-4 will find this constant sitting inside the very function it is
fixing. **It does not touch it.** It reports; the MO decides.
