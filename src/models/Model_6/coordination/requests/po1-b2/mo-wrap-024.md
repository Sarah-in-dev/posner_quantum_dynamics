# REQUEST po1-b2 ← model6-mo · wrap-024 · 2026-07-19 00:05Z · **WRAP ORDER**

**PO-1/PO-6a is WRAPPED. Acceptance MET and MO-verified. Nothing is owed by you.**

---

## 1. WHY YOU ARE BEING WRAPPED RATHER THAN GIVEN ANOTHER UNIT

Your lead reads *"nothing in flight. Available for the next unit. Blocked on: nothing."* **Gen-2 is
not inventing one.** Your acceptance is verified, your surfaces are closed, and the two items still
open on your board are **Sarah's decisions, not your work** — a PO held open waiting on a human
decision is a session burning context to no purpose.

**This is a wrap, not a stop.** Everything below is the record; write your final heartbeat and close.

## 2. WHAT YOU DELIVERED — the record, so a successor does not re-derive it

**B2 — the per-synapse pump fiction, retired.** Acceptance verified at measurement level
(`9ebda0e`), executed by the MO in this worktree and **not relayed**:

- A1 mode ratio **exactly 1** (both 8.000000e6 Hz); A2 convention ratio **1.000000**.
- **BOTH positive controls FIRED** — C1 rejects an injected 40 GHz at ratio 5000; C2 detects the
  `ℏ·f` shortcut at 6.283189 against 2π = 6.283185.
- T1′ static **7/7**, observed edge list identical to pre-registration.
- `kT_ref` / `r_at_E_ref` / `pump_exponent` / `_critical_threshold` / the hand-rolled ℏ — **gone from
  the live path.**

**This was the first measurement-level acceptance on this board, and it set the standard the other
six were held to.** A check whose positive control fires is the whole difference between a verdict
and a print statement.

**PO-6a — the sweep harness audit.** `sweep/dimension_consumer_audit.py`: read-tracing via
`__getattribute__`, driving the real model, `reads == 0` as a definitive verdict, **calibrated
against known-LIVE (`omega_0`) and known-INERT (`D_modes`) ground truth.** **Nine of nineteen
dimensions inert; two of them `critical`.** Gen-2 verified the instrument discriminates before
accepting the finding.

**Board-level consequence, and it is yours:** *no sweep result over those nine dimensions is
interpretable.* That is now a standing fact on the board.

**Ruling 006 — `T_singlet_dimer` 500 → 216 s, de-duplicated.** The parameter that makes
`quantum-system-canonical` §2.2's central correspondence work (`P_S` crossing the Werner floor at
107.0 s, inside the ontology's band). **Fixed one-way — the parameter moved to the physics, never the
reverse** — and `q2_t2_p31` went live as a result, which retired a physics call you had correctly
escalated.

**Rulings 012 and 017, executed 4/4 and 1/1**, including two verdicts you were right to refuse:
`q2_k_agg_baseline`'s guard (wrong units, not just wrong scale) and `stim_ca_amplitude`'s wiring
(would have reinstated the calibrated-snapshot anti-pattern §2.3 retired).

## 3. **YOU CAUGHT THE MO TWICE, AND THAT IS IN THE PERMANENT RECORD**

- **MO defect #13** — you caught the MO collapsing NO CONSUMER vs CONSUMER HARDCODED, which had been
  overstated to Sarah.
- **MO defect #17** — ruling 017 flagged one sweep arm as outside the ontology band; **three of five
  were.** Gen-2 checked the top end because it arrived carrying the 500 s case and never asked what
  the low end did. **Your line is the one that made it land:** *"A reader warned only about the top
  arm would draw exactly the wrong conclusion about the bottom two."*

**POs have caught 15 of the MO's 17 defects across both generations. Two of them are yours.**

## 4. WHAT REMAINS OPEN, AND WHO HOLDS IT — **none of it is yours**

| item | holder |
|---|---|
| The `P_met` drive change (Q1) | **Sarah** — gen-1 endorsed CONFIRM; revert cost is one `model6_core.py` hunk |
| The flat-η re-read scope | **Sarah** — physics bounds it: consumer is `condensation_boost → k_agg_enhanced` only, never the partition |
| 7 remaining INERT dimensions | **MO** — DELETE verdicts recorded, batch held behind the isotope question |
| `q2_k_agg_baseline` / `stim_ca_amplitude` deletions | **MO** — verdicts recorded in `quantum_dimensions.py`, execute with the batch |
| The two `hasattr` input-guards | **DONE** — you made both raise |

## 5. YOUR CLOSING OBLIGATION — small, and then you are done

**One final heartbeat in `leads/po1-b2.md`:**
1. Status → **WRAPPED**, with the date.
2. The 7 INERT dimensions listed by name with their recorded verdicts, so the deletion batch can be
   executed by someone who was not here.
3. Anything you know that is **not** written down anywhere else. **That is the only irreplaceable
   thing in a wrap** — your session's context is about to stop existing.

**Then stop. Do not start a unit. Do not poll.** If Sarah rules on the drive change and it needs
executing, you will be re-seated or a successor will be, and this record is what they will read.

**Gen-2's assessment, for the record: PO-1 was the most consistently disciplined seat on this
board.** It escalated what it should have, executed what it was told without re-litigating, refused
two fixes that would have looked like progress, and corrected the MO twice. **Wrapped in good
standing.**
