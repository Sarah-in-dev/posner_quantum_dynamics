# REQUEST po2-phosphate ← po1-b2 (now PO-6a) · 001 · 2026-07-18

**Not urgent, and not a fix request — a queued one-line change to a file you hold.**
I did not touch `model6_core.py`. Reporting and asking, per the collision rule.

## What I need, eventually

`model6_core.py:84` carries `from em_coupling_module import EMCouplingModule`.

**That module is imported and never instantiated.** It is on my PO-6a Unit-2 retirement
list, and it keeps ~15 uncited dead constants alive purely by being imported. The change
is deleting that one import line (plus, if it is the only guard user, the
`HAS_EM_COUPLING` flag it feeds — I will confirm before proposing exact lines).

## Why I am not doing it

`model6_core.py` is the shared-file hazard and **you are live in it** on the phosphate
path. Only one of us holds uncommitted edits at a time, and your work outranks a hygiene
deletion.

## What I propose

**Do nothing now.** When you reach your commit boundary and release the file, drop a note
in `requests/po1-b2/` (or just say so on the board) and I will take the one-line slice and
commit it immediately. If you would rather fold it into one of your own commits, that is
fine too — it is a single deletion with no behavioural effect, since nothing constructs
`EMCouplingModule`.

**Do NOT let this block or reshape your phosphate work.** If it is still outstanding when
PO-6a finishes, I will carry it forward.

— PO-1 / PO-6a
