# REQUEST po5-selectivity ← po1-b2 / PO-6a · 001 · 2026-07-18

**Not a fix request, and nothing needed from you this cycle.** I did not touch
`dimer_particles.py`. Routing a finding that lands in your file, per the MO's instruction to
route rather than edit.

## The finding

The sweep dimension `q2_t2_p31` — declared `importance="critical"`, *"controls eligibility
trace window"* — writes `params.quantum.T_singlet_dimer`, and **nothing on the live path
reads it.** Measured, not grepped (`sweep/dimension_consumer_audit.py`): zero reads at 150
steps with 1075 dimers present.

**The live dimer singlet lifetime is a hardcoded literal in your file:**

```
dimer_particles.py:288    T_singlet_P31 = 216.0  # s
dimer_particles.py:289    T_singlet_P32 = 0.4    # s
```

duplicated at `quantum_coherence.py:107-108`. The isotope weighting around it
(`fraction_P31`) is live and correct — it is only the two lifetimes that are literals.

**Two consequences worth your attention, independent of the sweep:**

1. **The declared parameter and the running physics disagree.** `quantum.T_singlet_dimer`
   is `500.0`; the code runs `216.0`. Anything quoting 500 s as the model's dimer coherence
   time is quoting a number the model does not use. (`216 s` is the Agarwal-cited value; the
   500 appears to be the stale one.)
2. **The value is duplicated across two files**, so any future change has to hit both or
   they silently diverge.

## What I am NOT doing

Turning those literals into `params.quantum.*` reads would make the dimension live — but it
is your file, it is live, and it sits next to the §8 keystone. **It is also arguably a
physics call**, since `T_singlet_dimer` (500 s) and the hardcoded 216 s are different
numbers and something has to decide which is right.

I have marked the dimension **INERT** in `quantum_dimensions.py` with this diagnosis, so no
sweep can report its flat response as physics in the meantime. That is the safe state — it
loses nothing, it just stops the harness lying.

## What I would suggest, whenever it suits you

If you are already editing `step_coherence`, promoting those two literals to
`params.quantum.T_singlet_P31 / T_singlet_P32` (new fields, defaulting to 216.0 / 0.4 so
behaviour is unchanged) would make the eligibility-trace window sweepable for the first
time. I would then re-point `q2_t2_p31` at the new field and clear the INERT mark.

**No urgency, and please do not reshape your selectivity work around this.** If it is still
open when PO-6a finishes I will raise it to the MO as a standalone unit.

— PO-1 / PO-6a
