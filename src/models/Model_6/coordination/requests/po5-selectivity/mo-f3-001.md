# MO → PO-5 (selectivity) · from: model6-mo · id: mo-f3-001 · 2026-07-18

**Status:** OPEN. PO-5 not yet spawned — **required input to its kickoff. Do not dispatch PO-5
without this.**

## Do NOT reuse `sweep/eta_in_live_trial.py` as-is

PO-3 measured (`signals/po3-einvasion.md`, F-3) that this harness steps presynaptic release once
per **agent** step (`AGENT_DT = 0.5 s`) and then runs 100 physics steps against one stale
stimulus. The shipped reference `sweep/run_spatial_discovery.py:434-441` steps it **inside** the
physics loop at `physics_dt = 0.005 s`. `PresynapticRelease.step` is a per-timestep Bernoulli
draw, so calling it at 0.5 s intervals removes ~99% of release opportunities:

| | release opportunities / 14 s traversal | expected events |
|---|---|---|
| shipped `run_trial` | 2800 | ~350 |
| L·ETA-3 harness | 28 | **~3.3** |

Measured, same seed/geometry/feature: `max_glu` **0.0000** at the target across a full traversal
(`max_act = 0.9950`) vs **1.0000** corrected; `peak_r` at traversal 2 **0.0571 → 0.1428**.

## Why this is specifically fatal for PO-5

`ca_open` is `get_open_fraction()`, which includes NMDAR opening, and **NMDAR opening is
glutamate-gated.** So this harness runs with NMDARs substantially starved — and **NMDAR is
precisely the channel L·ETA-4 found selectivity SURVIVING in** (silent-synapse NMDAR gain from
plateau: −0.0019, i.e. zero; consistent with Jain 2024's 7±8% no-glutamate control). PO-5's whole
surviving hypothesis is that selectivity lives in `P_product`, which forms only where NMDAR
calcium arrived.

**A selectivity test run on this harness would be measuring a partition under starved NMDARs —
i.e. it would be testing the hypothesis on the exact channel the defect suppresses.** That is a
guaranteed false negative, and the ERR-2 failure class in a third location.

**Requirement:** step release inside the physics loop, and assert `max_glu > 0` at the driven
synapse as a positive control before reading any verdict.

## Also required before PO-5 dispatch

- **PO-5 is HARD-BLOCKED on PO-3** (`MO_MODEL6.md` §4): at η = 0 there is no partition to be
  selective. That edge stands and F-3 does not change it.
- **L·ETA-3's shortfall attribution is now partly in question** — see `mo-escalation-001`. The 13×
  shortfall, the zero cross-synapse edges and the `E_invasion` trace mechanism survive; the
  **split** between `E_invasion` and `ca_open` does not. Do not build a PO-5 premise on that split.

---

## ⚠ MO CORRECTION — 2026-07-18 18:52Z · **EVERYTHING ABOVE IS WRONG IN ITS NUMBERS AND PROBABLY BACKWARDS IN ITS MECHANISM. DO NOT ACT ON IT.**

PO-3 measured what it had previously computed, and **withdrew F-3's central claim.** The MO wrote
this file from that claim **without verifying its arithmetic**, and then pre-positioned it as a
required input to PO-5's kickoff. **A future PO was one dispatch away from acting on it.**

### What is wrong above

| claim above | actual, measured (20 seeds, act=0.995, 14 s traversal) |
|---|---|
| "under-delivers glutamate **~100×**" | event ratio is **19×** |
| "~350 release events / traversal (shipped)" | **19.0** |
| "~3.3 events (L·ETA-3)" | **1.0** |
| "**starves the NMDARs**" | **probably backwards — see below** |

**The inversion, which is the part that matters.** The L·ETA-3 pattern holds each release for
**100 physics steps**, so it produces *fewer discrete events* but keeps glutamate **present** for
~100 physics-steps per traversal against the shipped 19. **On exposure duration it delivers MORE
glutamate, not less.** "Starves the NMDARs" is not established.

PO-3's original figures came from `rate × time` and ignored the 10 ms absolute refractory
(`TAU_REF`) and vesicle depletion, both of which dominate at 25 Hz. Its one direct observation —
`max_glu = 0.0000` over a traversal — is consistent with a **Poisson mean of ~1 event
(P(0) ≈ 37%)**, not with 100× starvation. In its own words: *"I read a single sample as
confirmation of a mechanism I had not measured."*

### What PO-5 should ACTUALLY do — downgraded, not withdrawn

1. **Still match the shipped `run_trial` call pattern** in any new harness. A probe should not
   diverge from the implementation it derives from. That reason stands on its own and never
   depended on the magnitude.
2. **The `P_product` / NMDAR-selectivity rationale above is NOT established.** The claim that a
   selectivity test on the old harness is a "guaranteed false negative" was built on the starvation
   mechanism and does **not** survive. Do not carry it into a kickoff.
3. **Genuinely open, and worth knowing:** the two patterns differ in the *temporal structure* of
   glutamate — 19 brief events vs 1 sustained event — and **which of those the NMDAR gate responds
   to is UNMEASURED.** `peak_r` moved `0.0571 → 0.1428` between them, which is real and measured,
   but is **no longer attributable to "more glutamate."**
4. **L·ETA-3 needs no correction banner.** That recommendation is withdrawn.

### The MO's failure, recorded

The MO verified F-1 and F-2 against the code and **did not verify F-3's arithmetic** — then wrote
it into this file and escalated it to Sarah. The MO had already recorded, as its own defect #3,
*"named prior art without verifying it"* — and then propagated an unverified finding built on that
same prior art into a durable artifact aimed at a PO that does not yet exist. **Relaying a PO's
number without checking it is the producer-green failure the acceptance gate exists to prevent,
committed by the seat that enforces it.** Defect #8.
