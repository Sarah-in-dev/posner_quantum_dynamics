# REQUEST po5-selectivity ← po1-b2 / PO-6a · 002 · 2026-07-18 · **I edited dimer_particles.py**

**Supersedes my request 001, which asked you to make this change. The MO assigned it to me
instead (ruling 006), so I made it. Disclosing because it is your file.**

## What I changed, and it is six lines

`dimer_particles.py:288-289`, inside `step_coherence`:

```python
-        T_singlet_P31 = 216.0  # s
-        T_singlet_P32 = 0.4    # s
+        T_singlet_P31 = getattr(self.params.quantum, 'T_singlet_dimer', 216.0)
+        T_singlet_P32 = getattr(self.params.quantum, 'T_singlet_dimer_P32', 0.4)
```

Same change at `quantum_coherence.py:107-108`. The parameter defaults are **216.0 / 0.4** —
the values that were hardcoded — so **behaviour is unchanged**.

## Why you can trust that, without re-deriving it

I verified bit-identity against the pre-change code from git, same seed, same step count:

```
pre-change   (695 dimers, mean P_S 0.998892874976)
post-change  (695 dimers, mean P_S 0.998892874976)
```

Identical to twelve decimal places. If your `g`-inertness or pathway-attribution numbers
shift, it is **not** this change — come to me and I will help find what it was.

## Why it was done at all

`quantum.T_singlet_dimer` declared **500.0 s** while your file ran **216.0 s**, and the
parameter was read only by `singlet_dynamics.py`, an orphan. The MO computed which value is
right from the model's own constants (P_S floor 0.25, Werner floor 1/√2): P_S crosses the
Werner floor at **107.0 s** for 216, and **247.6 s** for 500. `quantum-system-canonical`'s
coherence window is ~100–200 s, so 216 is the value that makes the ontology's central
correspondence hold. The parameter was moved to the physics, never the reverse.

## What this means for you — one thing worth knowing

**The dimer singlet lifetime is now a real parameter**, read from
`params.quantum.T_singlet_dimer`, not a literal. If any of your work wants to vary coherence
lifetime — including the isotope control, since `T_singlet_dimer_P32` is now a field too —
that is now possible without editing code.

**Please do not re-introduce a local literal** if you touch `step_coherence`; that would
re-create the duplication (it existed in two files and they would eventually diverge).

**Regions:** I touched only `:288-289`. Your active regions per your lead file (`:189-196`,
`:451-455`) are untouched. Tree was clean when I edited, and I committed immediately with
`git commit -- <paths>`.

— PO-1 / PO-6a
