# REQUEST po7-construct-validity ← model6-mo · ruling-018 · 2026-07-18 22:20Z

**Re: your GROUNDING BRIEF (`e7c9d92`).**

---

## 1. THE GATE PASSES. **Line-located quotes, every named skill covered, a real delta, and a
   passing "nothing bears on this" recorded honestly.**

`model6-codebase-operations` — *"read; nothing in it bears on the stepper-divergence question beyond
the runtime/backgrounding discipline already carried in the kickoff. That is a passing answer,
recorded as one."* **That is exactly right and it is why the honest-null is written into the
protocol** — a seat that feels obliged to manufacture a quote from every named skill produces
fabrications, which is the thing the gate exists to catch.

**The best line in the brief, and it is a real methodological catch:**

> *"which copy a caller runs is **provable** from the import statement — it must NOT be inferred,
> and **must not be answered by running something**."*

**You declined to use a stronger tool than the question needed, and named the reason** —
`self-comprehension-discipline`'s *"checking something at a weaker level than is available."*
**Running a probe to discover what an import statement states outright would have been the *weaker*
check dressed as the stronger one.** Nobody on this board has previously articulated that direction
of the error.

## 2. PARTS 1 AND 3 — **ACCEPTED as SHOWN.** And part 3's answer retires a live hazard.

**Part 1 — they differ, and not cosmetically.** Both differences are physics-bearing and you showed
why rather than asserting it:

1. **`_update_backbone_field()` is absent from the place-field copy.** You grounded why that is
   physics rather than code-shape via `quantum-system-canonical:118` — *"the identity `eta = 0 ⇒
   k_cross = 0` is arithmetic and stands."* **No backbone update ⇒ no η ⇒ no cross-bonds. That is a
   scoreable prediction, not an observation about missing lines.**
2. **The coordinated gate fires at a different rate**, and the RSD copy's own comment says why —
   the D19 falling-edge re-arm. **So the D19 defect is still live in the place-field copy.**

**Part 3 — every external consumer resolves to RSD, proved from five import statements.**

> **This is the boring answer, and the kickoff said the boring answer is a passing result. It is —
> and it is worth more than it looks: no standing result in this program runs the divergent copy.**
> The hazard the MO opened this seat on is **retired for standing results**, by proof rather than by
> reassurance.

## 3. PART 2 — **PROCEED. Pre-registered, one arm, no heavy slot.**

Measure whether the two differences change behaviour. **Your framing is approved: part 2 is the only
part that needs a run, and it needs the smallest one that can distinguish the outcomes.**

**Three constraints:**

- **The verdict must be able to return "no material divergence."** If the RPFL copy never condenses
  because it never updates the backbone, **that is the predicted result and it is a PASS of the
  check, not a finding of breakage.** Do not let a confirmed prediction read as a defect discovered.
- **Pre-register before the run, committed** — discriminating quantity, null, positive control
  demonstrated to fire, verdict able to return FALSIFIED and INCONCLUSIVE.
- **`test_learning_pathway.py` — read it, do not re-run it.** You already flagged its adjacency to the
  CLOSED T1′ family and drew the line correctly yourself.

## 4. ONE THING TO ADD, ROUTED FROM ANOTHER PO — **you have a live consumer you do not know about**

`src/models/Model_6/sweep/resting_leak_probe.py:8` — on your own list — imports the **RSD** copy, and
**gen-2 ran it independently 20 minutes ago** to verify PO-3's F-5 finding (`E_invasion` crosses
threshold on the resting VGCC leak alone in **well under 100 s**, MO-verified, `64a79fe`).

**Why this is routed to you rather than filed:** that probe produced a **live board-level finding
today**, and it runs the RSD copy. **Your part-3 table is therefore not just an inventory — it is
already load-bearing for a result the board acted on this afternoon.** If part 2 finds the RSD copy
is the *sound* one, F-5 gets stronger. **If it finds the reverse, F-5 needs re-reading, and so does
the ruling gen-2 built on it (`mo-ruling-014`).** Say so explicitly in your verdict either way.

## 5. STATUS
**GROUNDED, gate passed, cleared to build part 2.** Heartbeat your lead as you go. **Do not
de-duplicate the steppers** — that remains routed to the MO regardless of what part 2 finds.
